import React from 'react';
import { ISelector } from '../Forms/Input/SelectInput';

interface Props {
    onChange: (e: ISelector) => void;
}

const SelectPoli: React.FC<Props> = (props: Props) => {
    return (
        <div></div>
    )
}

export default SelectPoli;